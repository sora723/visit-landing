---
description: V2 freeform landing — 작업 체크포인트 (재개용)
alwaysApply: true
---

# V2 작업 체크포인트 (2026-07-24)

브랜치: **`feature/v2-freeform-landing`**

## 이 파일의 역할

의도적 프로젝트 재개 규칙: `.cursor/rules/v2-checkpoint-2026-07-23.mdc`

## 완료

1. siteCode V1 · rendererVersion · V2 스키마/경계 · media 컬럼 계약
2. V2 noindex metadata
3. **읽기 전용** `v2.page.published` Apps Script + Next `fetchV2PublishedPage`  
   (Sheet 탭 미생성 · clasp push/deploy 안 함 · page.tsx는 여전히 V2Placeholder)

## 다음에 하면 안 되는 것

Sheet 탭 생성 · clasp push · Netlify 배포 · Preview · V2 UI 렌더러 · page.tsx Placeholder 교체 — **별도 지시 전 금지**

## 재개 시

V2 safe page / renderer에 `fetchV2PublishedPage` 연결 — 사용자 지시 대기
