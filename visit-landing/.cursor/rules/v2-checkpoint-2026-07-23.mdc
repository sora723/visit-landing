---
description: V2 freeform landing — 작업 체크포인트 (재개용)
alwaysApply: true
---

# V2 작업 체크포인트 (2026-07-24)

브랜치: **`feature/v2-freeform-landing`**

## 이 파일의 역할

의도적으로 추가한 **프로젝트 재개용 규칙** (Cursor 자동 생성물 아님).  
경로: `visit-landing/.cursor/rules/v2-checkpoint-2026-07-23.mdc`

## 완료

1. siteCode V1 유지 · rendererVersion 분기 · V2 스키마/경계
2. 미디어: URL은 `V2_콘텐츠` 컬럼만 · options는 재생 플래그만
3. V2 noindex: `resolveHomeRobotsMetadata` + page `generateMetadata`
4. site live config: React `cache` + 60s TTL + in-flight dedupe  
   → metadata/page 별도 호출해도 동일 요청에서 원격 Apps Script는 사실상 1회

## 미구현 (별도 지시 전 금지)

Sheet · Apps Script · V2 API · 렌더러 · Preview · submit
