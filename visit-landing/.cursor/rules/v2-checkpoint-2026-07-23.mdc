---
description: V2 freeform landing — 작업 체크포인트 (재개용)
alwaysApply: true
---

# V2 작업 체크포인트 (2026-07-24)

브랜치: **`feature/v2-freeform-landing`**

## 이 파일의 역할

의도적 프로젝트 재개 규칙: `.cursor/rules/v2-checkpoint-2026-07-23.mdc`

## 완료

1. siteCode V1 · rendererVersion · V2 스키마/경계 · media 컬럼 계약
2. V2 noindex metadata
3. 읽기 전용 `v2.page.published` + Next `loadV2PublishedPage` / `fetchV2PublishedPage`
4. idempotent V2 Sheet schema inspect/ensure (Apps Script 수동)
5. **page.tsx**: `rendererVersion===v2` → Published loader → `V2PublishedPageShell` / `V2SafeStatePage`  
   (블록 디자인 미구현 · Preview/Publish 없음)

## 다음에 하면 안 되는 것

clasp push · Netlify 배포 · Preview · Draft · Publish · Rollback · 실제 블록 UI 렌더러 — **별도 지시 전 금지**

## 재개 시

V2 블록 컴포넌트 레지스트리 → `V2PublishedPageShell`에 연결 — 사용자 지시 대기
