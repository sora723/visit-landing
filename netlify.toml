# visit-landing monorepo — Netlify base directory
[build]
  base = "무제 폴더/visit-landing"
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
