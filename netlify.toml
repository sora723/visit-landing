# visit-landing — Netlify base via ASCII symlink (visit-landing → 무제 폴더/visit-landing)
[build]
  base = "visit-landing"
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
